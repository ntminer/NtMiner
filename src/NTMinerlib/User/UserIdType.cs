namespace NTMiner.User {
    /// <summary>
    /// 目前UserIdType都是LoginName，Email和Mobile尚无用户入口。
    /// </summary>
    public enum UserIdType {
        LoginName,
        Email,
        Mobile
    }
}
