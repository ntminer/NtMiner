<Window 
    x:Class="NTMiner.Views.ConsoleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:app="clr-namespace:NTMiner;assembly=AppModels"
	xmlns:controls="clr-namespace:NTMiner.Controls;assembly=NTMinerWpf"
    mc:Ignorable="d"
    ResizeMode="NoResize"
    Background="Black"
    WindowStyle="None"
    Height="260" 
    Width="520"
    WindowStartupLocation="CenterScreen"
    Title="原版内核控制台窗口">
    <Grid>
        <Grid x:Name="Splash" Background="{StaticResource IconFillColor}">
            <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Width="120" Height="120" Source="{x:Static app:AppStatic.BigLogoImageSource}">
                </Image>
                <StackPanel Margin="10 0 0 0" VerticalAlignment="Center">
                    <TextBlock Text="NTMiner" FontSize="50" Foreground="{StaticResource ThemeForeground}"></TextBlock>
                    <TextBlock Text="{x:Static app:AppStatic.VersionFullName}" FontSize="20" Foreground="{StaticResource ThemeForeground}"></TextBlock>
                </StackPanel>
            </WrapPanel>
            <controls:KbSystemCloseButton HorizontalAlignment="Right" VerticalAlignment="Top" Height="32" />
        </Grid>
        <Rectangle 
            x:Name="ConsoleBgRectangle"
            HorizontalAlignment="Right"
            VerticalAlignment="Top" 
            Fill="#01000000">
        </Rectangle>
    </Grid>
</Window>
